name: Docker Image CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build the Go Docker image
      run: docker build images/go/. --file images/go/Dockerfile --tag go-ci:$(cat images/go/VERSION).$(date +%s)
